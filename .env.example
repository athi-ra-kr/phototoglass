# Copy this file to `.env` on the SERVER ONLY (never commit the real .env — it's gitignored).
# Local development needs none of this; sensible dev defaults are built into settings.py.

SECRET_KEY=replace-with-a-long-random-string
DEBUG=False
ALLOWED_HOSTS=production.photo2glass.mu,168.144.157.5
